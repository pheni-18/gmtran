from setuptools import setup


setup(
    name='gmtran',
    version='1.0.0',
    install_requires=[],
    entry_points={
        "console_scripts": [
            "gmtran = main:main"
        ]
    }
)
